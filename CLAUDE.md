# Claude Instructions

When working on resolvers (adding, editing, or debugging any file under `src/calculators/`), read `docs/architecture.md` first — it documents the resolver pattern, context shape, and design rules that govern this codebase.
